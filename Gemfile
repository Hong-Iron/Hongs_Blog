source "https://rubygems.org"

# Pin to the exact gem set GitHub Pages builds with, so a local
# `bundle exec jekyll serve` matches what GitHub will render.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ no longer bundles these by default.
gem "webrick", "~> 1.8"
